import React from "react";

const Header = () => (
  <header style={{ padding: "1rem", textAlign: "center", background: "#1e90ff", color: "white" }}>
    <h1>QuickCart</h1>
  </header>
);

export default Header;